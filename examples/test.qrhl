isabelle.

quantum var x : bit.
quantum var y : bit.
quantum var aux : int.
classical var a : bit.

program p1 := {
  { local x; y <q ket a; x <q ket 1;  }
}.

program p2 := {
  { local x; y <q ket a; x <q ket 0; }
}.

qrhl {top} call p1; ~ call p2; {(⟦x2,y2⟧ ==q ⟦x1,y1⟧) ⊓ (⟦y2,x2⟧ ==q ⟦y1,x1⟧)}.
 inline p1.
 inline p2.
 equal.


