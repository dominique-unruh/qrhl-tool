isabelle.

quantum var q : bit.
quantum var r : bit.
quantum var s : bit.
classical var x : bit.

program Q := {
  local s;
  on s apply hadamard;
}.

program p := {
  local x,q;
  r <q undefined;
  on q,r apply comm_op;
  call Q;
}.


qrhl {top ⊓ Qeq[r1=r2]} call p; ~ call p; {Qeq[r1=r2]}.
 inline p.

 equal exclude Q.

simp!.
simp.
inline Q.
equal.
simp.
skip.
simp.
skip.
simp.
qed.
