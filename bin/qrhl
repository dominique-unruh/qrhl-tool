#!/bin/bash

set -e

SELF="$(readlink -f "$0")"
DIR="$(dirname "$SELF")"
DIR="$(dirname "$DIR")"
( cd  "$DIR" && setsid sbt assembly )
export QRHL_FORCE_BUILD=1
java -Dorg.slf4j.simpleLogger.defaultLogLevel=debug -jar "$DIR/qrhl.jar" "$@"
